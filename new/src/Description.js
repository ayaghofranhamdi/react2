// MonComposant.js
import React from 'react';
import product from './products';

// Définir le composant fonctionnel
const Description = () => {
  return (
    <div>
      <h1>Une souris est un dispositif de pointage pour ordinateur. Elle est composée d'un petit boîtier fait pour tenir sous la main,</h1>
    </div>
  );
};

// Exporter le composant pour pouvoir l'utiliser ailleurs
export default Description;
